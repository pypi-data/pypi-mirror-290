[project]
name = 'adipri-opensource'
dynamic = ["version"]
readme = "README.md"
description = 'adipri-opensource'
requires-python = ">= 3.10"
authors = [
  { name = "Siddheshwar Kamble", email = "siddhukamble45@gmail.com" },
]


classifiers = [
    "Programming Language :: Python :: 3",
    "License :: Other/Proprietary License",
    "Operating System :: OS Independent",
]

dependencies = [
    "fastapi==0.111.0",
    "sqlalchemy==2.0.30",
    "uvicorn==0.30.1",
    "pytest==8.2.2",
    "aiosqlite==0.20.0",
    "greenlet==3.0.3"
]

[project.optional-dependencies]
dev = [
    'pytest',
    'pytest-cov',
    'pytest-asyncio',
    'pytest-mock',
    'uvicorn[standard]'
]


[project.scripts]
adipri-opensource = "adipri-opensource.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.hatch.build.targets.wheel]
packages = ["adipri-opensource"]
exclude = ["adipri-opensource/tests/"]


[tool.hatch.build]
include = ["adipri-opensource/*"]

[tool.hatch.build.targets.sdist]
exclude = ["adipri-opensource/tests/"]

[tool.hatch.version]
path = "adipri-opensource/VERSION"

