[project]
name = "this_platform"
version = "0.5.0"
description = "🖥️ Get this platform"
authors = [
    { name = "alexdelorenzo", email = "3579286+alexdelorenzo@users.noreply.github.com" }
]
dependencies = []
readme = "README.md"
requires-python = ">= 3.8"

[project.scripts]
platform = "this_platform:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/this_platform"]

