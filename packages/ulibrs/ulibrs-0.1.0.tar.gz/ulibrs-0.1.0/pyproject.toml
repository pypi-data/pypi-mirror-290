[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "ulibrs"
version = "0.1.0"
description = "A simple but universal library written in Rust."
authors = [{ name = "raugfer" }]
license = "MIT"

[tool.maturin]
features = ["pyo3/extension-module"]
