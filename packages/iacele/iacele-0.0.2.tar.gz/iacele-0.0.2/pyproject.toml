[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "iacele"
version = "0.0.2"
authors = [{ name = "Emilio Costich Pérez", email = "ecostichp@gmail.com" }]
description = "Librería para crear BI en la administración de la empresa LA CASA DEL CARPINTERO"
readme = "README.md"
license = { file = "LICENSE" }
dependencies = ["pandas"]

[tool.setuptools]
package-dir = { "" = "src" }
