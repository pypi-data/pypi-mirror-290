[project]
name = "crypto_chassis_trade"
version = "0.0.2"
dependencies = ["aiohttp ~= 3.9.4"]
requires-python = ">= 3.10"
readme = "README.md"
license = { file = "LICENSE" }

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["crypto_chassis_trade"]
