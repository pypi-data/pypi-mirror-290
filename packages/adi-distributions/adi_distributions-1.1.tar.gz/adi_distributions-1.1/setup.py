from setuptools import setup

setup(name='adi_distributions',
      version = '1.1',
      description='Gaussian and Binomial distributions',
      packages=['adi_distributions'],
      author = 'Adithya Chintala',
      zip_safe=False)