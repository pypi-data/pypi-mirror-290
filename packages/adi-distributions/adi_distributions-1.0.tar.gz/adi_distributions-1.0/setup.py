from setuptools import setup

setup(name='adi-distributions',
      version = '1.0',
      description='Gaussian and Binomial distributions',
      packages=['adi-distributions'],
      author = 'Adithya Chintala',
      zip_safe=False)