[tool.poetry]
name = "smoothiepy"
version = "0.0.2"
description = "A library for various easing functions."
authors = ["Strimnik"]
license = "MIT"
packages = [{ include = "smoothiepy" }]

[tool.poetry.dependencies]
python = ">=3.6"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"