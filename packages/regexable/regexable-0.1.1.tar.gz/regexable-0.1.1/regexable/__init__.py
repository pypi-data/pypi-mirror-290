# regexable/__init__.py

# Import the core class so it can be easily accessed
from .core import Regexable

# Define what gets imported when someone uses `from regexable import *`
__all__ = ["Regexable"]
