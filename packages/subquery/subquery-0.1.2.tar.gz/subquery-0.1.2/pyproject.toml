[tool.poetry]
name = "subquery"
version = "0.1.2"
description = "A package to generate subqueries and follow-up questions"
authors = ["andthattoo <omer@firstbatch.xyz>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
ollama = "^0.3.1"
torch = "^2.4.0"
transformers = "^4.44.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"