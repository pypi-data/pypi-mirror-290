from setuptools import setup, find_packages

setup(
    name="pysynchronizer",
    version="0.0.5",
    packages=find_packages(),
    python_requires=">=3.7",
    install_requires=[

    ],
    extras_require={

    }
)